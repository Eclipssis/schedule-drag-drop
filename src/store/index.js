import Vue from "vue";
import Vuex from "vuex";
import schedule from "@/store/schedule";

Vue.use(Vuex);

export default new Vuex.Store({
  state: {},
  mutations: {},
  actions: {},
  modules: { schedule }
});
